# Variables de entorno de la aplicación Kustoc - EJEMPLO
# Copia este archivo como .env para configurar tu entorno

# Configuración de entorno (establecer a production para entorno de producción)
NODE_ENV=production

# Forzar modo producción (incluso si NODE_ENV no está configurado correctamente)
FORCE_PRODUCTION=true

# Puertos para desarrollo local
PORT=3001
FRONTEND_PORT=5173

# Configuración de base de datos
DB_PATH=./server/kustoc.db

# Configuración SSL para producción
SSL_KEY_PATH=./privkey.pem
SSL_CERT_PATH=./fullchain.pem
